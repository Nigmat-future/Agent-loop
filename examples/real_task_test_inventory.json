{
  "task_type": "general",
  "objective": "Read tests/test_service.py and tests/test_interfaces.py, then write reports/test_inventory.md summarizing the current automated checks.",
  "context": {
    "source_files": ["tests/test_service.py", "tests/test_interfaces.py"],
    "deliverable_path": "reports/test_inventory.md",
    "instructions": [
      "Inspect both test files before writing.",
      "The report must contain the headings '# Test Inventory', '## Service Tests', and '## Interface Tests'.",
      "Mention at least one covered behavior from each file."
    ]
  },
  "success_checks": [
    {"kind": "file_exists", "target": "reports/test_inventory.md"},
    {"kind": "file_contains", "target": "reports/test_inventory.md", "expected": "# Test Inventory"},
    {"kind": "file_contains", "target": "reports/test_inventory.md", "expected": "## Service Tests"},
    {"kind": "file_contains", "target": "reports/test_inventory.md", "expected": "## Interface Tests"}
  ],
  "budget": {
    "max_steps": 7,
    "max_shell_calls": 0,
    "max_http_calls": 0,
    "timeout_seconds": 30,
    "max_errors": 3
  },
  "permissions": {
    "workspace_root": ".",
    "enable_file": true,
    "enable_shell": false,
    "enable_http": false,
    "allowed_shell_prefixes": ["python", "echo"],
    "allowed_http_domains": []
  }
}
